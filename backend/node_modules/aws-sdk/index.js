class DocumentClient {
  get() { return { promise: async () => ({}) }; }
  put() { return { promise: async () => ({}) }; }
  update() { return { promise: async () => ({ Attributes: {} }) }; }
}
module.exports = {
  DynamoDB: { DocumentClient },
  config: { update: () => {} }
};
